<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper" >
    <insert id="addDish" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name,category_id,price,image,description,status)
        values (#{name},#{categoryId},#{price},#{image},#{description},#{status})

    </insert>
    <insert id="addFlavor">
        insert into dish_flavor
        <foreach collection="flavors" item="f" separator=",">
            (dish_id, name, value)
        VALUES (#{f.dishId},#{f.name},#{f.value})

        </foreach>
    </insert>
    <select id="dishPageQuery" resultType="com.sky.vo.DishVO">
        SELECT dish.*,dish_flavor.`name` 
            as categoryName FROM dish JOIN dish_flavor 
                on dish.id = dish_flavor.dish_id
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="name != null">
                and dish.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and status = #{status}

            </if>
        </where>
        order by dish.create_time desc
    </select>
</mapper>